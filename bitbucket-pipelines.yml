image: ubuntu:24.04
pipelines:
  branches:
    develop:
      - variables:
        - name: SSH_PORT
          default: 22
      - step:
          script:
            - ssh -p $SSH_PORT $SSH_TARGET
            - whoami